services:
  - type: web
    name: ticket-search-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: TRAVELPAYOUTS_TOKEN
        sync: false